# Session Log — 2026-05-27 (Session 3)

## Goal
Fix New York data issue; update public-facing content; begin GitHub Pages deployment planning.

## Completed

### New York data fix
- Investigated NY rows in `data/raw/iaff_extracted/ny.json`
- Identified that cancer (§ 363-d, expired July 1, 2005) and respiratory (§ 363-f, expired July 1, 2008) were coded as `presumption_exists: false / presumption_type: "expired"`
- Fetched NY RSS § 480, which extends all temporary retirement benefits expiring 1974–2011 — same mechanism already noted in the cardiovascular rows
- Updated both rows: `presumption_exists: true`, `presumption_type: "statute"`, `needs_verification: true`
- Updated notes to explain the § 480 extension and flag uncertainty (original IAFF source characterized both as inactive)
- Added `needs_verification` boolean field to the data model
- Updated `app.R` `make_collapsed_table()` to prepend `"* Verify active status. "` to Notes column when `needs_verification == true`
- Re-ran full pipeline: `4_iaff_combine.r` → `5_iaff_export_json.r` → copy to `shiny-app/data/` → `shinylive::export()` → `quarto render`

### About page rewrite (`website/about.qmd`)
- Repositioned IAFF as primary data source with direct link
- Framed app as structured interface over the IAFF database
- Demoted NCCI and Brandt-Rauf (2024) to cross-check/supplementary role
- Preserved legal disclaimer; added mention of `*` verification flag

### Landing page rewrite (`website/index.qmd`)
- Explicit prototype framing
- Plain-language explanation of presumptive laws
- ALERRT description and research mission
- IAFF credited as primary data source with link
- How-to-use instructions covering both Occupation and Condition modes

## Pipeline reminder
After any data edit, run in order:
```
Rscript analysis/code/4_iaff_combine.r
Rscript analysis/code/5_iaff_export_json.r
cp website/data/presumptive_laws.json website/shiny-app/data/presumptive_laws.json
cd website && Rscript -e "shinylive::export('shiny-app', 'dashboard', overwrite=TRUE)"
cd website && quarto render
```

### Project moved from OneDrive to GitHub directory
- Copied project from OneDrive shared library to `~/Documents/GitHub/PROJ_presumptive_laws`
- Created `.gitignore` (excludes `website/_site/`, `website/dashboard/`, `**/.quarto/`, `docs/lit/` PDFs, `.claude/settings.local.json`)
- Initialized git, made initial commit (227 files), pushed to `github.com/ALERRT-Research/PROJ_presumptive_laws`
- Created `deploy.sh` — builds Shinylive + renders Quarto + force-pushes `_site/` to `gh-pages` branch
- Updated `bob.md` with new path, repo URL, sync method
- Updated `~/.claude/bob/registry-local.md` with project entry

### GitHub Pages deployment
- Ran `deploy.sh` — site built and pushed to `gh-pages` branch
- GitHub Pages enabled; site live at `https://alerrt-research.github.io/PROJ_presumptive_laws/`

### Data source corrections (6 files)
Repositioned IAFF as primary data source throughout all project documentation:
- `README.md` — updated Data Sources section
- `project-brief.md` — updated Data Plan and Prior Work sections
- `CLAUDE.md` — updated Data Sources, Known Data Issues, and pipeline instructions
- `.claude/rules/domain-profile.md` — updated Primary data sources list
- `data/codebook.md` — noted v1 superseded by v2; IAFF as primary source
- `analysis/code/1_extract_data.r` — marked as superseded in header

### Operating procedure established
- Peter confirmed: always ask before `git commit` or `git push` — never push autonomously
- Saved to project memory (`memory/feedback_git_push.md`)

## Next session
- Front-end design polish (deferred from session 2)
- Update suggested citation URL in `index.qmd` and `about.qmd` now that the live URL is known
